{
  "name": "jcgrading-paving-solution-site",
  "version": "1.0.0",
  "description": "Static marketing site for JC Grading Paving Solution, Inc. This package.json exists only to compile the production Tailwind CSS build (assets/css/tailwind.build.css) — nothing here runs at request time. Deploy the repo as static files; node_modules is not needed in production.",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i build/tailwind-input.css -o assets/css/tailwind.build.css --minify",
    "watch:css": "tailwindcss -c tailwind.config.js -i build/tailwind-input.css -o assets/css/tailwind.build.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.19"
  }
}
